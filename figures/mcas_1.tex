\begin{figure}
\begin{lstlisting}[numbers=left]
let clear cass is_after =
  if is_after
  then List.iter (fun cas -> cas.state.before <- cas.state.after) cass
  else List.iter (fun cas -> cas.state.after <- cas.state.before) cass

let finish /(gid)/ casn status = match casn.status with
  | Before -> false
  | After -> true
  | Undetermined cass as old_status ->
      let is_after = status == After in
      if /!Zoo.resolve_with (!/
           /*Atomic.Loc.compare_and_set [%atomic.loc casn.status] old_status status*/
         /!) casn.proph (/(gid)/, is_after)!/
      then clear cass is_after ;
      casn.status == After

let rec determine_as casn cass =
  /(let gid = Zoo.id () in)/
  match cass with
  | [] -> finish /(gid)/ casn After
  | cas :: continue as retry ->
      let { loc; state } = cas in
      /(let proph = Zoo.proph () in)/
      let old_state = /*Atomic.get loc*/ in
      if state == old_state then
        determine_as casn continue
      else if /(Zoo.resolve proph ()/state.before == eval old_state/())/ then
        lock casn loc old_state state retry continue
      else
        finish /(gid)/ casn Before
and lock casn loc old_state state retry continue = match casn.status with
  | Before -> false
  | After -> true
  | Undetermined _ ->
      if Atomic.compare_and_set loc old_state state
      then determine_as casn continue
      else determine_as casn retry
and eval state =
  if determine state.casn then state.after else state.before
and determine casn = match casn.status with
  | Before -> false
  | After -> true
  | Undetermined cass -> determine_as casn cass
\end{lstlisting}
\caption{MCAS: Implementation (1)}
\label{fig:mcas_1}
\end{figure}